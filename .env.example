# Database Configuration (Production)
# These environment variables are required when running in production mode

HOST=your-database-host.com
PORT_DB=5432
DATABASE=your_database_name
USERNAME=your_database_username
PASSWORD=your_database_password
SSL_MODE=Require
COMMAND_TIMEOUT=30
APPLICATION_NAME=expense_backend

# API Port (Render will set this automatically)
PORT=8080
